# Chemistry Companion images

Drop real product screenshots here when available, e.g.:

- workspace.png
- properties.png
- batch.png

Until then the site uses branded React UI mocks in `src/components/product/ProductMock.tsx`.
